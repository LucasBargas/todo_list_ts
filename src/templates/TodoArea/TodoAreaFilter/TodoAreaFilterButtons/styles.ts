import styled from 'styled-components';

export const TodoAreaFilterButtonsContainer = styled.nav`
  ul {
    display: flex;
    gap: 1.5rem;
    padding-top: 0.75rem;

    li {
      width: 100%;
    }
  }
`;
